# Copy to .env and fill values for production
# Never commit the real .env or SECRET keys to source control.

# Django settings
DJANGO_SECRET_KEY=changeme
DJANGO_DEBUG=False
DJANGO_ALLOWED_HOSTS=yourdomain.com

# Database (use Render's provided value if using managed Postgres)
DATABASE_URL=postgres://user:pass@host:5432/dbname

# Optional: other envs
# DJANGO_ADMIN_USER=admin
# DJANGO_ADMIN_PASSWORD=secret
