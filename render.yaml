services:
  - type: web
    name: theher-webapp
    env: python
    runtime: python-3.11.7
    region: oregon
    plan: free
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PORT
        value: 10000
